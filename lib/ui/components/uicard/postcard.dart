import 'package:flutter/material.dart';

class PostCard extends StatelessWidget {
  var userIcon;
  var userName;
  var postDetail;
  var userTags;
  PostCard({this.userIcon, this.userName, this.postDetail, this.userTags});
  @override
  Widget build(BuildContext context) {
    return Card(

    );
  }
}
